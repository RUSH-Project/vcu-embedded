source [find interface/jlink.cfg]
transport select swd

source [find target/stm32g4x.cfg]
proc _erase_ {} {
    init
    reset init
    flash erase_address 0x8000000 0x00080000
}
proc _flash_ {elf_filepath} {
    program $elf_filepath verify reset
}
